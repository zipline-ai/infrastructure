{{- $systemNamespace := include "zipline-orchestration.computeSystemNamespace" . }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zipline-orchestration.fullname" . }}-loki
  namespace: {{ $systemNamespace }}
  labels:
    {{- include "zipline-orchestration.labels" . | nindent 4 }}
    app.kubernetes.io/component: loki
data:
  loki.yaml: |
    auth_enabled: false

    server:
      http_listen_port: 3100

    common:
      path_prefix: /loki
      replication_factor: 1
      ring:
        kvstore:
          store: inmemory
      {{- with .Values.compute.loki.commonStorage }}
      storage:
        {{- toYaml . | nindent 8 }}
      {{- end }}

    {{- with .Values.compute.loki.storageConfig }}
    storage_config:
      {{- toYaml . | nindent 6 }}
    {{- end }}

    schema_config:
      configs:
        - from: "2024-01-01"
          store: tsdb
          object_store: {{ .Values.compute.loki.objectStore | default "filesystem" }}
          schema: v13
          index:
            prefix: index_
            period: 24h

    limits_config:
      allow_structured_metadata: true
      retention_period: {{ .Values.compute.loki.retention | default "168h" }}
      ingestion_rate_mb: 32
      ingestion_burst_size_mb: 64
      per_stream_rate_limit: 16MB
      per_stream_rate_limit_burst: 32MB

    compactor:
      working_directory: /loki/compactor
      compaction_interval: 10m
      retention_enabled: true
      retention_delete_delay: 2h
      delete_request_store: {{ .Values.compute.loki.deleteRequestStore | default "filesystem" }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zipline-orchestration.fullname" . }}-loki
  namespace: {{ $systemNamespace }}
  labels:
    {{- include "zipline-orchestration.labels" . | nindent 4 }}
    app.kubernetes.io/component: loki
spec:
  type: ClusterIP
  ports:
    - port: 3100
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: loki
{{- if .Values.compute.loki.storage.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "zipline-orchestration.fullname" . }}-loki
  namespace: {{ $systemNamespace }}
  labels:
    {{- include "zipline-orchestration.labels" . | nindent 4 }}
    app.kubernetes.io/component: loki
spec:
  accessModes:
    - ReadWriteOnce
  {{- with .Values.compute.loki.storage.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.compute.loki.storage.size | default "10Gi" | quote }}
{{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zipline-orchestration.fullname" . }}-loki
  namespace: {{ $systemNamespace }}
  labels:
    {{- include "zipline-orchestration.labels" . | nindent 4 }}
    app.kubernetes.io/component: loki
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: loki
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: loki
    spec:
      serviceAccountName: {{ include "zipline-orchestration.serviceAccountName" . }}
      securityContext:
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
      {{- with .Values.compute.loki.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.compute.loki.tolerations }}
      tolerations:
        {{- toYaml .Values.compute.loki.tolerations | nindent 8 }}
      {{- else }}
      tolerations:
        - key: "kubernetes.io/arch"
          operator: "Equal"
          value: "arm64"
          effect: "NoSchedule"
      {{- end }}
      containers:
        - name: loki
          image: {{ .Values.compute.loki.image | default "grafana/loki:3.4.2" }}
          args:
            - -config.file=/etc/loki/loki.yaml
          ports:
            - name: http
              containerPort: 3100
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            {{- toYaml .Values.compute.loki.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/loki
            - name: storage
              mountPath: /loki
      volumes:
        - name: config
          configMap:
            name: {{ include "zipline-orchestration.fullname" . }}-loki
        - name: storage
          {{- if .Values.compute.loki.storage.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "zipline-orchestration.fullname" . }}-loki
          {{- else }}
          emptyDir:
            sizeLimit: {{ .Values.compute.loki.storage.size | default "10Gi" }}
          {{- end }}
