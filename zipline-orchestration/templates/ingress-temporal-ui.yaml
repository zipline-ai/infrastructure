apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ if .Values.domains.temporal }}temporal-ui-ingress{{ else }}temporal-ui-ingress-nip{{ end }}
  namespace: zipline-system
  labels:
    {{- include "zipline-orchestration.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    kubernetes.io/ingress.global-static-ip-name: {{ .Values.staticIPs.temporalUI | quote }}
    networking.gke.io/managed-certificates: {{ if .Values.domains.temporal }}"temporal-ui-ssl"{{ else }}"temporal-ui-ssl-nip"{{ end }}
spec:
  rules:
    - host: {{ include "zipline-orchestration.temporalUIDomain" . }}
      http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: temporal-web-service
                port:
                  number: 80